Update the docs to reflect a **Modular AI (Microservices)** architecture option as a switch/toggle.

**Core Concept Changes:**

1. **System 1 (Fast Gear):** Replace LLM-based labeling/intents with `Transformers.js` (running locally in Node.js).
2. **System 2 (Slow Gear):** Restrict `Llama.cpp` strictly to dialogue, social reasoning, and complex planning.
3. **Layer 2 (Perception):** Use `Xenova/all-MiniLM-L6-v2` for all vectorization (events, items, and structures).
4. **Summarization:** Use a small encoder-decoder model `Xenova/t5-small` to condense episode logs.
5. **Caching:** Implement a "Vector Cache" strategy—the system should check PostgreSQL for existing concept vectors before running any model inference.

- Add `@xenova/transformers` to the `package.json` requirements.
- Ensure models like `all-MiniLM-L6-v2`, `t5-small` ,`distilbert-base-uncased-mnli` (Intent Router), and `bert-base-multilingual-cased-ner-slavic` (Entity Recognition) are initialized on server start.

Implement a summarization function using `Xenova/t5-small`.

- **Goal:** Take a list of raw event strings from a sealed Episode and condense them into a 1-sentence "Subjective Memory" for the database.
- **Example Usage:**

JavaScript

```jsx
const summary = await summarizeEpisode(
  "Steve broke 5 logs. Steve crafted planks. Steve built a wall.",
);
// Returns: "Steve collected wood and began construction."
```

- **Layer 2 Revision:** Remove manual [C, V, I, S, X] math. Replace it with a "Semantic Vector" generated by `all-MiniLM`.
- **Layer 3 Revision:** Add "Fast Intent Routing." If the `distilbert` model detects an "Aggression" or "Simple Trade" intent, the system should bypass Layer 6 (LLM) and send an `IntentPacket` directly to Layer 7.

**Labels:** `["aggression", "trading", "building", "asking_question", "idling"]`.

**Logic:** If a high-confidence intent is found (e.g., aggression), bypass the LLM and return an immediate `IntentPacket`.

**Example Usage:**

```jsx
const intent = await classifyIntent("Why are you following me?");
if (intent.label === "asking_question") {
  // Ping Llama.cpp for a dialogue response
}
```

- **NER Implementation:** Use the Slavic NER model to extract specific block types or item names from player chat commands (e.g., "Build a [Wall] out of [Cobblestone]").
- **Performance Targets:** Update target latencies:
  - Perception (MiniLM): <20ms
  - Intent Routing: <50ms
  - Dialogue (Llama.cpp): 2-4s (Unchanged)

**Constraints:**

- **Deduplication:** Explicitly state that models should only run on _new_ concepts; everything else is a DB lookup.
- **Modular Design:** Ensure the "Brain" logic is separated into `intent_router.js`, `vector_engine.js`,`episode_summerizer.js`and `llm_client.js`.

"Implement an `AI_MODE` toggle in the backend config. When set to `MONOLITHIC`, use the manual 5D vector logic (C,V,I,S,X). When set to `MICROSERVICES`, use `Transformers.js` to generate 384D vectors. Update the PostgreSQL schema to include both `vector_manual` and `vector_minilm` columns to support this toggle without requiring schema changes." this `AI_MODE` toggle is supposed to also change the responsibilities of llama.cpp, when its `MONOLITHIC` , llama.cpp is the main source and has multiple responsibilities, but when switched to `MICROSERVICES` , we take load off the LLM by giving some of its responsibilities to the models from Xenovas `Transformers.js`. I want a switch to toggle between what we have now and using these microservices.

I also would like to expand the `DEBUG_MODE` .

### 1. The Inference Logger

Implement a function `logInferenceTrace(layer, modelName, result, confidence)` that triggers only when `DEBUG_MODE` is true.

- It should format the "Brain Path" into a readable string for the Minecraft `actionbar`.
- **Example Output:** `L3: [DistilBERT] -> Intent: Building (0.89)`

### 2. Vector Similarity Highlighting

Add a debug function to visualize the **Vector Engine** (Layer 2) results.

- When a villager "Perceives" a structure, the system should send a command back to Minecraft to play a particle effect at the `anchor_x/y/z` of the recognized structure.
- In the logs, show the **Cosine Similarity** score between the observed block cluster and the closest match in the `concepts` table.
- player should also see event vectors in the Modal UI menu

### 3. Manual Concept Correction (The "Teacher" UI)

Create a debug UI modal logic where a player can "correct" the AI.

- If the villager mislabels a structure (e.g., calls a "Tower" a "House"), the player can type the correct label.
- **Backend Task:** This should trigger a `RE-VECTOR` event where the `all-MiniLM` model generates a new embedding for that cluster with the _new_ label and updates the `concepts` table. This is how the villager "learns" from the player in real-time.

### 4. Performance Benchmarking

- Log the "Gear Latency" for every event:
  - `Fast Gear (MiniLM/BERT): 12ms`
  - `Slow Gear (Llama.cpp): 3400ms`
- If `Fast Gear` exceeds 50ms, trigger a "Performance Warning" in the debug console.

"Create a config_router.js in NodeDB with endpoints to GET and POST the current DEBUG_MODE and AI_MODE states. In the Minecraft Script API, implement a chat listener or scriptevent that sends a fetch request to these endpoints so I can toggle the entire architecture's behavior using in-game commands."
